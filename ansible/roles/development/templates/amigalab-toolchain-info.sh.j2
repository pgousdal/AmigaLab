#!/usr/bin/env sh
set -eu

printf '%s\n' "AmigaLab toolchain prefix: {{ toolchain_prefix }}"
printf '%s\n' 'Use the versioned helper scripts in the AmigaLab repository to install GCC, VBCC, VASM, and VLINK.'
printf '%s\n' 'No Amiga SDK files, ROMs, or Workbench media are installed by this helper.'
