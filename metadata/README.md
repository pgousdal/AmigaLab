# AmigaLab metadata

This repository defines the independent metadata layout used at runtime under
`/srv/amigalab/metadata`. Preservation metadata must never be written into a
source collection. The runtime directories are `collections`, `objects`,
`sources`, `imports`, and `verification`.
