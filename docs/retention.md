# Retention

Retention is conservative and disabled by default. `retention-plan` reports
disposable candidates; execution requires `--yes`. Preserved collections,
acquired media, provenance, events, approved plans, and completed transactions
are never candidates. Ambiguous or active files are retained. Plans and
reports are canonical metadata and can be reviewed offline.

