# Aminet readmes

Readmes are opened read-only and indexed with bounded text size. UTF-8,
ASCII, Windows-1252, and ISO-8859-1 are attempted conservatively. Raw text,
selected fields, encoding, warnings, and parser confidence are derived for
search; original files and hashes are never changed.
