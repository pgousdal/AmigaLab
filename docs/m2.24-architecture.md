# M2 completion architecture

Canonical metadata and preserved files feed deterministic catalog documents,
then SQLite FTS5. The local web interface presents those documents and
canonical trace/report references read-only. Meilisearch is an optional,
rebuildable accelerator; `auto` fallback remains SQLite-first. No web request
approves plans, executes operations, edits metadata, or serves file bodies.
